options(shiny.autoreload = TRUE)
# Watch only hand-edited source files. NOT .json — the author app rewrites www/articles.json on
# startup, and watching it would trigger an endless reload loop.
options(shiny.autoreload.pattern = "\\.(r|R|htm|html|js|css)$")
